# Optional local speech recognition. No CUDA or API key required.
faster-whisper==1.2.1
sounddevice==0.5.6
Pillow==12.3.0

# Local speech noise suppression (model downloaded during setup; no cloud audio service).
deepfilter-stream==0.1.0
